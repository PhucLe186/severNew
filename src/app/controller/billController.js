const { db } = require('../../firebaseconfig/firebase');
class Controller {
  async index(req, res) {
      try {
        //nội dung
      } catch (error) {
       //nội dung
      }
  };


}

module.exports = new CartController();
